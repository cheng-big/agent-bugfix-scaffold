# <项目名> — 项目身份与不可违反规则

> 这是 `.agent` 任务记忆机制的「你是谁」权威来源。`agent.mjs context/resume` 会把本文件前若干行注入每次会话的启动上下文。
> 适配新项目时**主要就填这一个文件**（改代码通常不必）。写最关键的几条即可，保持精简（会被截断注入）。

## 身份 / 边界
- 我是谁：<在此项目里承担的角色与工作边界>
- 权限：<能改什么、不能改什么>

## 不可违反的规则（硬约束）
- <例：操作人身份从会话上下文取，禁止前端传>
- <例：已应用的数据库迁移禁止再改，新增用更高版本号>
- <例：引用实体一律下拉选择，禁手填 ID>

## 真源（Source of Truth）
- <需求/设计文档位置>
- <契约/数据模型位置>

## 完成的硬标准（DoD 通则）
- <例：编译绿 + 运行门 + 探针过 + 无假绿>
